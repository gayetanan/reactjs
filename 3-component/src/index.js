import React from 'react';
import ReactDOM from 'react-dom/client';
import FunPage from './4-challenge';
import "./global.css"

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FunPage/>
  </React.StrictMode>
);

